import { pageMetadata } from "@/lib/page-metadata";

export const metadata = pageMetadata("components/media-surface");

export default function MediaSurfaceLayout({ children }: { children: React.ReactNode }) {
  return children;
}
