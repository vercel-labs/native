import { pageMetadata } from "@/lib/page-metadata";

export const metadata = pageMetadata("media-producers");

export default function MediaProducersLayout({ children }: { children: React.ReactNode }) {
  return children;
}
