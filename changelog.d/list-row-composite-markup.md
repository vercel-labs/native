feature: **Markup list-items take element children (the list-row composite)**: `<list-item>` now accepts element children in place of its text run — the children flow horizontally inside the row's flat chrome (transparent at rest, full-width hover/selected washes, one hit target), so composite rows like a notes list's title + snippet or a folder rail's icon + name + count stay honest list rows instead of bordered panel boxes. Mixing a text run with element children is a teaching error in the validator and both engines; name composite rows with `label`.
- **Showcase lists go flat**: the notes example's folder rail and note list are list-row composites now, and the feed's timeline rows trade their bordered panel chrome for the same flat list wash — lists read as lists, and card chrome stays an explicit opt-in (the kanban board's cards document it).
