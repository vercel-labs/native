fix: **`native dev` runs the Debug dev loop**: dev now defaults to `-Doptimize=Debug` (pass an optimize flag to override), so the markup hot-reload watcher and the Debug-only teaching diagnostics are actually compiled into the binary the dev loop runs — a release-mode dev run silently shipped a binary that never reloads. `native build` keeps its ReleaseFast default.
- **The armed watch is observable**: the automation snapshot header now carries `markup_watch=armed|off`, so a dev loop can prove editing a `.native` source will reload the app instead of bisecting an unarmed watcher.
