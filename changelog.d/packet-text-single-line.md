fix: **Packet text honors engine line breaks** (friction #80): GPU frame packets now carry the layout's measured lines for every text command and the macOS packet rasterizer draws them verbatim, so tight intrinsic boxes no longer re-wrap host-side ("Song" splitting into "Song"/"s", "4:33" into "4:3"/"3") when AppKit's line breaker disagreed with the measurement that sized the box.
