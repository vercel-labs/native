feature: **Deck example (`examples/deck`)**: the radically skinned sibling of the soundboard — the same conceptual music player (the identical 48-track library, simulated playback through a repeating timer effect, queue, search, seek, transport, context-menu `pbcopy`) wearing Winamp-era rack-unit hardware identity in the true two-window shape: a SMALL, FIXED player window (460x180, `resizable = false`, hidden-inset titlebar whose chrome-drawn gold cap band is the `window_drag` region, traffic lights cleared by the live `on_chrome` inset) and a matching playlist rack unit as a model-declared second window (`windows_fn` + `window_view`, racked in/out by the chunky PL key or `primary+L`, user close round-tripping through `on_close`). The skin is a dark-only chassis-black theme (one phosphor-green hue, mono-first readouts, square 1–3px corners, compact density, per-control visual tokens) plus a fixed-count `UiApp.chrome` display-list pass that machines the device — an AI-generated brushed-plate texture registered through the runtime image channel and drawn as one `draw_image` UNDER the vector machining (gold cap band, bevels, ridged grip band, corner screws, recessed control wells, CRT scanlines, glare, and a seven-segment elapsed readout as sheared glowing paths); the playlist rack lays a second generated carbon-weave texture behind its content through the `image` widget leaf — no widget or engine code touched. The VFD carries a deterministic title marquee (a pure function of the playback clock, like the 32-band `ui.chart` spectrum); the playlist's status strip is comptime-compiled `.zml` with engine-parity coverage. `zig build test-example-deck` runs transport/queue/search dispatch through the fake effects executor, the playlist window's full round-trip through real dispatch (open by toggle, drive the ledger from the second canvas, close by Msg and as the user), spectrum + marquee determinism, texture registration and the codec-less pure-vector fallback, chrome command-count stability, the dark-only + high-contrast-fallback theming contract, automation click-through on the transport, and layout budgets at both fixed window sizes.
