feature: **Deck example (`examples/deck`)**: the radically skinned sibling of the soundboard — the same conceptual music player (the identical 48-track library, simulated playback through a repeating timer effect, queue, search, seek, transport, context-menu `pbcopy`) wearing a Winamp-era rack-unit hardware identity: a dark-only chassis-black theme (one phosphor-green hue, mono-first readouts, square 1–3px corners, compact density, per-control visual tokens) plus a fixed-count `UiApp.chrome` display-list pass that sculpts the machine — brushed chassis texture, gold cap band and brand plate, raised/inset bevels, ridged grip bands, corner screws, recessed control wells, CRT scanlines and glare over every glass, and a seven-segment elapsed readout drawn as sheared glowing paths — no widget or engine code touched. The faceplate is Zig-drawn chrome (VFD glass readout with scaled mono spans, a live 32-band spectrum analyzer as one `ui.chart` bar-series widget driven by a deterministic function of the playback clock, transport cluster, output fader + meter); the status strip is comptime-compiled `.zml` with engine-parity coverage; a `PERF` face scales the analyzer full-deck. Registered app shortcuts (primary+P/←/→/K, Escape) map to typed messages. `zig build test-example-deck` runs transport/queue/search dispatch through the fake effects executor, spectrum determinism (same state → same bars, pause freezes, idle floor), the dark-only + high-contrast-fallback theming contract, automation click-through on the transport, and layout budgets.
