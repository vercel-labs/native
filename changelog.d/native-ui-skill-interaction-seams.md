improvement: **The native-ui skill teaches the interaction seams**: a "Select, then act" section covers `on_double_press` (additive press-then-double firing, Zig views only) and row-level Enter (`on-submit` on `list-item` as the row's primary action while Space keeps select), a "Keys" section covers the two focus registers, the quiet-list-row rule, and the `Options.on_key` app-level fallback with its pinned precedence, and the style-token section names the Zig-only `quiet_hover` knob — agents now reach the new seams from the skill alone.
- **A `playlist-row-actions` eval case pins it**: a fresh agent must reach row-level `on-submit` and the app-level key fallback from the skill, graded by deterministic greps plus the live snapshot (seeded rows, model-driven selection, derived idle status); the eval README also documents the three cases added with the composer round.
