improvement: **The Geist pack's underline tabs adopt the measured 24px inter-trigger gap**: a new `tabs_gap` control metric token (default 0, override tier like the rest of the ladder) that the `.underline` tab register reads when the author leaves the strip's gap at 0 — the pack states the reference strip's measured 24px flex gap, an author-stated gap still wins, and the house `.pill` register never reads the metric, so no-pack layout is byte-identical by construction (house catalog and preview signatures unmoved). The register's tokens-laid-out geometry is now machine-verified through a pinned per-scheme tab-strip specimen alongside a house pin of the same specimen.
