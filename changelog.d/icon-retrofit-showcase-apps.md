improvement: **Vector icons in the pre-icon showcase apps**: soundboard and markdown-viewer, built before the icon system landed, now use it — the soundboard transport is real icon buttons (chevron-left / play–pause / chevron-right via the overlay idiom: the button is the hit target, the never-hit-tested `<icon>` stacked over it is decoration), the track-row "▶" text glyph is the crisp `play` vector (plus a muted `pause` on the loaded-but-paused row), search clear is an `x` icon button, and Back to albums / Play album are icon+text buttons; the markdown-viewer editor pane is marked with the `edit` pencil and its theme toggle drops the "◑"/"◐" dingbats that rendered as tofu in the bundled font (plain "Dark mode"/"Light mode" until sun/moon icons exist).
