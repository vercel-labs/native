fix: **CLI/app automation protocol handshake — stale binaries fail loudly instead of driving yesterday's dropbox**: both binaries bake `automation.protocol.version` at their own build time, the app stamps it into every snapshot header (`protocol=N`), and the CLI refuses snapshot reads, waits, asserts, and command queues against a live publisher on any other version — naming both versions and pointing at the stale side. `native version` now prints the build commit and protocol version (`native 0.3.0 (commit abc1234, automation protocol v1)`), and `markup check`'s unknown-element/attribute errors add the other explanation out loud: "your `native` binary may be stale". Snapshots without a `protocol=` field (pre-handshake publishers) are refused with the same teaching error.
