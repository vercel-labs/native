fix: **Reference glyph pen walks the measurement seam** (system-monitor friction): the CPU reference renderer advanced glyphless text with the raw estimator even when an injected measure provider (CoreText on live macOS) had measured the line's bounds and clip, so every multibyte codepoint's flat 0.65em estimate overshot the real advance and pushed one trailing glyph per multibyte codepoint past the clip in automation screenshots of live apps. The pen now uses the same provider-aware `measureTextAdvance` layout, caret, and selection geometry use; the estimator path is bit-identical. The showcase apps' trailing-space workarounds are removed.
