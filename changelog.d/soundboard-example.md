feature: **Soundboard example (`examples/soundboard`)**: a showcase-grade music-library browser — album grid with generated cover art through the runtime image pipeline, track lists with native context menus (Play Next, Copy Title via a `pbcopy` spawn), a now-playing bar with simulated playback on a repeating timer effect, live search, and a custom light/dark theme — authored markup-first (`header.zml`/`nowplaying.zml` compiled at comptime) with Zig views only where the closed grammar can't reach (square cover images, grid columns, scaled headings, per-row context menus). Ships with a deterministic suite (fake-executor playback/seek/copy paths, strict-decoder cover registration, theming, engine parity) wired into `test-examples-native` as `test-example-soundboard`.
