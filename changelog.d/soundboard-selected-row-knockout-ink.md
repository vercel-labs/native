fix: **Soundboard selected-row ink follows the accent in both schemes**: the selected track row's title, subtitle, track number/state icon, and duration now take the accent's knockout ink (`accent_text`, the same white the filled Play Album button wears) instead of the window-background token, which was only white in the light scheme — in dark mode the ink resolved near-black and sank into the pink fill.
