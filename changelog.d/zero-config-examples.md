improvement: **Zero-config showcase examples**: fourteen showcase apps (calculator, notes, kanban, feed, habits, soundboard, deck, markdown-viewer, system-monitor, gpu-surface, gpu-dashboard, gpu-components, canvas-preview, effects-probe) dropped their build.zig/build.zig.zon and now ship exactly what `native init` scaffolds — app.zon + src + assets — with the `native dev|test|build` verbs driving the CLI-generated build graph; root example suites, the GPU smokes, and the Linux/Windows CI smokes drive the same verbs, and every example that still owns a build.zig opens with the reason.
