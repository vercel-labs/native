feature: **Icon registry expansion (20 → 41)**: the curated built-in stroke-icon set gains the showcase + GitHub-app vocabulary — `folder`, `folder-open`, `save`, `file-text`, `sun`, `moon`, `eye`, `clock`, `music`, `volume`, `shuffle`, `repeat`, `skip-back`, `skip-forward`, `archive`, `refresh-cw`, `send`, `arrow-up`, `arrow-down`, `git-pull-request`, `circle-dot` — every name available in markup (`<icon name>`, `<button icon>`) and Zig views (`Ui.icon`, `ElementOptions.icon`), with the validator mirror, LSP, docs, and skill lists updated in lockstep.
- **Path-element budget raised for icons** (`max_canvas_path_elements_per_view` 128 → 2048): vector icons are path commands (~10–25 lowered elements each), and a sidebar of icon rows plus an icon toolbar blows 128 immediately — the notes example did the moment its folder rows wore icons. 2048 also gives path-drawn charts real room (the system-monitor sparklines had to become bars under the old cap); memory math documented in `canvas_limits.zig`.
