fix: **Offscreen screenshots clear with live tokens** (system-monitor friction): `renderCanvasScreenshot` cleared with the last *presented* clear color, so a theme change without an intervening presented frame captured a stale background. Widget display-list emissions (every rebuild, theme changes included) now record the declared clear color from live tokens, so theme-pair screenshot scripts no longer need to present a frame per theme.
