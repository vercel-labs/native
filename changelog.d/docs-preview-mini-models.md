improvement: **Docs live previews respond like real apps**: scenes that need model-owned state declare a tiny shared mini-model (`SceneModel` — boolean slots, a selected index, an open flag) and the wasm host routes the REAL widget event dispatch (press, toggle, dismiss, keyboard activation) through each scene tree's typed handler table into `update`, rebuilding on every Msg — so the accordion expands and collapses with the chevron flipping, the tabs switch their panel, the dialog dismisses on click-outside and reopens from a button, and the select opens its anchored dropdown and picks an option. Theme swaps now REBUILD the scene (token-resolved styles like muted text re-resolve for the new scheme), and the engine's cursor channel is exported (`preview_cursor`) and mirrored onto the canvas's CSS cursor — I-beam over text entry, pointer over pressable controls, col-resize over sliders.
