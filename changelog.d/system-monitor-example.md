feature: **System monitor example (`examples/system-monitor`)**: a showcase live CPU/memory/process monitor and the effects-channel proof — a repeating `fx.startTimer` tick spawns `ps`/`vm_stat` through collect-mode `fx.spawn` (Linux reads `/proc/meminfo` behind a comptime switch), pure fixture-tested parsers turn the stdout into stat tiles with 60-sample sparkline bar charts (Zig-view chart drawing, one token-tinted bar widget per sample), uptime read from pid 1's etime, and a top-CPU process table with search, sort toggles, and a context-menu Terminate that sends SIGTERM only after a real confirmation dialog (no SIGKILL anywhere). Pause/resume drives the timer; in-flight ticks are skipped and counted, never overlapped. Cool teal/slate "ops room" theme in both modes. `zig build test-example-system-monitor` runs the committed-real-output parser fixtures, the whole sampling loop through the fake effects executor with TestClock timestamps, the history ring, kill and clipboard flows, theming, markup parity, automation snapshot assertions, and an exact-frame tile layout check.
