fix: **Docs wasm previews: time flows, animations play, shortcuts route, tiles present at logical size**: the page's `performance.now()` now also feeds the runtime clock seam (freestanding builds read a host-fed monotonic clock instead of a frozen 0), and `preview_render` reports dirty while ANY render animation is active plus one settling frame — so a clicked switch animates its knob and the rAF loop parks exactly when motion ends, instead of freezing mid-travel until the next unrelated repaint.
- **Keyboard shortcuts stay in the preview**: the canvas key handler forwards and `preventDefault`s the Cmd/Ctrl chords the engine consumes — Cmd/Ctrl+A selects all inside the focused engine text field (the same synthetic key_down the desktop edit menu emits) instead of selecting the docs page, and cut/copy/paste hit the engine clipboard.
- **DOM blur reaches the engine**: a new `preview_set_focused` entry point mirrors the canvas element's focus, so a blurred preview drops its ring, caret, and blink animation and the render loop parks instead of pumping forever.
- **Tiles present at logical size**: the preview tile is capped at the scene's logical CSS width (the vocab records 2x file pixels; the backing store scales by devicePixelRatio in the blit), so controls render at their native size instead of stretched to the prose column.
- **Badge copy**: the live pill reads "WASM Preview" instead of "Live".
