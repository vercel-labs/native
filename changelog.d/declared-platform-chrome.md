feature: **Declared platform chrome — real native tab bars from shell metadata (iOS first)**: apps declare a tab set and optionally one primary floating action in shell metadata (`ShellConfig.chrome` in a Zig scene, `.shell.chrome` in app.zon: tabs carry a command id, a label, and an icon-vocabulary name), and the toolkit iOS host projects the declaration as REAL native controls — an actual system tab bar and a real button, styled and made accessible by the OS, never imitated in canvas. Tab icons are the app's own vocabulary glyphs (built-ins or registered `app:` icons), rasterized through the canvas vector core into template images the system tints, and the bar's overlap folds into the safe-area insets so layout clears it through the existing `on_chrome` channel. Selection lives in the MODEL: taps dispatch the declared command ids through the embed command path into `on_command`, the new `selected_tab_fn` derivation drives which item the bar shows active, and the whole loop replays deterministically from the Msg journal. Hosts without a projection leave the declaration honestly inert (desktop ignores it; Android projection is a later round), and the chrome channel's `tabs_projected` flag lets an in-canvas switcher yield while a native bar owns the affordance. The mobile-canvas and mobile-shell examples adopt the vocabulary, and the soundboard's compact shell gains the canonical music-app tab bar (Albums/Songs) whose in-canvas switcher yields on iOS.
- **Host-reported form factor on the chrome channel**: `WindowChrome.form_factor` carries the host's size class (the iOS host reports its horizontal size class, re-reporting on trait changes), so apps switch shells on host truth with width derivation as the fallback — the soundboard's `Model.formFactor` now prefers the host report, exactly the replacement its switch-site comment promised, pinned by tests including a Msg-journal determinism run across both channels.
