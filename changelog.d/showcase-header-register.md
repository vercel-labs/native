improvement: **Showcase header bands carry only working controls**: the app-name badge and product caption are gone from every showcase header — a window never labels itself. Notes leads with search and trails its New note action, soundboard keeps tabs + search, system-monitor keeps its live sampling status, kanban/habits trail their one primary action, ui-inbox shows Clear done by presence (only when something is done), feed keeps just its load counter, and gpu-dashboard's toolbar drops the drawn title so the mode control leads the band. Titles appear only where they inform (the markdown viewer's current document); all captures regenerated.
