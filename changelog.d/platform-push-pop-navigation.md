feature: **Platform push/pop navigation (iOS first)**: apps whose model owns a page stack project it through two options beside `selected_tab_fn` — a `navigation_depth_fn` derivation (0 = the root page, 1 = one push in; tab switches are lateral, never depth) and a `navigation_back_command` id — and the toolkit iOS host presents REAL platform transitions from it: the depth is polled once per tick, a grown depth runs the system push (the outgoing page is the previous frame retained from the damage path, zero extra renders; the incoming state renders through the ordinary damage delivery before the animation starts, so both sides are pixel-correct for the transition's whole run and the live canvas swaps back in at completion), a shrunk depth runs the system pop, and a depth change arriving with a tab change reconciles with no transition. The interactive edge-swipe-back is the system's own recognizer with its real physics, armed exactly while a page is open: completion dispatches the declared back command through the embed command path exactly once (the same journal entry the app's back button produces), cancellation dispatches nothing and the model is untouched. The MODEL owns navigation state throughout — the projection is a pure derivation, so a journal replayed without a host produces the identical model — and the projection is honestly inert where nothing presents it (desktop ignores it; Android's system back will ride the same command in a later round). The soundboard's compact shell adopts it: album grid → album detail pushes, back button and edge swipe both pop through one `close_album` Msg.
