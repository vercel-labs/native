feature: **Window-action effects**: `fx.closeWindow(label)` and `fx.minimizeWindow(label)` drive the REAL OS window verbs from `update` — labels resolve against the live window table at call time, close runs the runtime's existing close path (last-window exit semantics included), and minimize is a new platform seam (`minimize_window_fn`) implemented on all three desktop hosts (macOS `miniaturize:`, Windows `SW_MINIMIZE`, GTK `gtk_window_minimize`). Fire-and-forget with a test-observable mirror on the channel; the seam behind app-drawn window controls on chromeless windows.
