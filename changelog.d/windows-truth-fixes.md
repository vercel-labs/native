fix: first real-Windows truth pass fallout: embed input timestamps use the runtime clock (they read 0 on Windows before), untyped connect failures from the Windows net layer classify as `connect_failed` instead of `protocol_failed`, `native doctor` reports the WebView2 runtime honestly (and stops implying a wired Chromium desktop engine on Windows), and the tooling path tests compare separator-agnostically.
