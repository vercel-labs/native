improvement: **The notes editor is the pane**: the open note renders as text on the pane's own field — the textarea's control chrome dissolves into the background tokens, one quiet meta line replaces the Copy/Delete button row, and note-list rows lead with the title and a trailing age, the snippet taking the full second line.
- **Notes row context menus, all markup**: right/ctrl-click on a folder or note row presents a context menu — Rename/Delete on folders (the Rename button is gone), Copy/Delete on notes, Restore/Delete Permanently in Recently Deleted — with typed per-row Msgs, drivable end to end by automation. (Now declared with the `<context-menu>` element and presented by the platform — see the context-menu entry.)
- **Notes Recently Deleted**: deleting a note (or a folder — its notes move rather than vanish) stamps a deleted timestamp; the sidebar scope exists only while trashed notes do, trashed notes open read-only with a Restore action, and only Delete Permanently removes a record. Store format v2 carries the state; v1 stores load cleanly with every note live, proven by a migration test.
- **Notes folder dialog autofocus**: New Folder and Rename land the keyboard in the name field the moment the dialog opens — type, Enter, done.
