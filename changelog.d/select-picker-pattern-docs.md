improvement: **`select`/`combobox` pickers are documented honestly**: the skill and LSP now say what the engine actually ships — trigger controls with no options attribute — and teach the sanctioned composition (model-owned open state, `dropdown-menu` of `menu-item`s under an `if`, inside a `stack` to overlay), so pickers stop falling back to toggle-button rows by accident. A true anchored popup channel stays design-noted rather than half-shipped.
