fix: **Mono runs and dynamic symbols render honestly on every text path.** The "exper i ment al _" wreckage in inline code and mono table cells lived in the reference renderer (the CPU path behind every `automate screenshot` and mobile embed), not the live packet path — the packet host already draws whole runs with the real mono face through the system text stack. Three seams closed: (1) the reference renderer now CENTERS each bundled-sans outline inside its fixed 0.6 em mono cell instead of left-aligning it, so mono runs read as monospace ("gh  aut h  l ogi n" -> "gh auth login"; no mono face is bundled, and that stays true); (2) span-run text commands now carry the measurement seam (`text_layout` with `wrap = .none` + the injected provider), so per-cluster repaints walk the same advances layout positioned the runs with — a provider-kerned prose run can no longer overpaint the mono span after it and swallow the inter-span space ("remainingexperimental_"); (3) uncovered symbol/pictograph codepoints (U+2190–U+2BFF: GitHub's ☐/☑ ballot boxes, ⌘, geometric shapes) charge a 0.8 em estimator advance — the AppleSymbols cascade class live macOS text actually falls back to (measured 0.83 em) — instead of the 0.6 em `.notdef` advance. The live no-tofu guarantee for dynamic strings is CoreText's cascade, now pinned by a macOS test shaping the bundled face; the reference renderer keeps its documented `.notdef` block, and markdown deliberately does NOT rewrite symbol characters (bytes stay intact for selection/copy; the debug coverage diagnostic still names them).
