feature: **Windows host window seams reach the AppKit contract**: app timers (markup hot-reload and press-hold now work on Windows), appearance events (dark scheme from the apps theme preference, reduce-motion from the animation setting, high contrast), main-WebView layering (a layered shell app previously reported an error on Windows), window options at create (resizable, hidden titlebar styles, content min-size floors via WM_GETMINMAXINFO, and content-sized frames instead of outer-sized), and interactive window moves from the widget drag channel.
