feature: **`info` — the violet fourth semantic hue** (friction #85): `ColorTokens` gains `info`/`info_text`, the identity slot beside success/warning/destructive that GitHub-shaped apps need for a merged-PR badge (violet in all four themes, matching the status tokens' contrast discipline; overridable like every other token). The markup token vocabulary, validator message, LSP docs, skill, and docs list it, so `foreground="info"` works everywhere a color token does.
