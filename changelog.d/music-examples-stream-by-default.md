improvement: **The music examples stream out of the box**: the committed manifests now ship a hosted `.url_base` (the exact bytes `tools/prepare-example-music.sh` writes), so a fresh clone runs the soundboard or deck with zero setup — browse the full catalog AND play it, streaming on demand and caching locally on first play. `NATIVE_SDK_MUSIC_URL_BASE` still overrides at launch (set it empty to disable streaming), prepared local files still win over the stream, and the "assets not prepared" / `NO MEDIA` notices now appear only when there is genuinely no way to play: no local files and no URL base.
