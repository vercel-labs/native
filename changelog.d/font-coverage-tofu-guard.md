feature: **Font-coverage tofu guard**: a codepoint outside the bundled face's cmap (⌘, ✓, ⑂, dingbats, CJK) renders as a tofu box everywhere the bundled outlines are the only glyph source — reference screenshots, mobile embeds — and kept reappearing one glyph at a time. Markup literals are now checked statically: `native markup check` and the LSP flag literal text content and text-bearing attribute literals (labels, placeholders, timeline copy, stepper steps) with a teaching error at the exact character (the CLI names it: `(found "⌘" U+2318)`), while `{binding}` spans stay the runtime's job. Dynamic strings and Zig-authored text get a Debug-build `zero_canvas_ui` diagnostic naming the codepoint when the view builds — never a build failure, since live macOS falls back through CoreText. The coverage predicate is a new std-only `font_coverage.zig` over the same embedded TTF (lockstep-tested against the renderer's cmap), so the std-only validator can share it. Deliberate scope: the engines still ACCEPT such documents (`markup check`/LSP are the lint homes; hard engine failures would brick shipped apps whose live path renders fine), and `zig build validate` stays manifest-only — markup rides `markup check`, Zig strings ride the Debug warn. Found and fixed real framework tofu on the way: the stepper's completed ✓ and the catalog's ✓/✗ indicators now ride the vector icon channel. Text-node positions in markup diagnostics also improved: they now point at the run's first visible character instead of its end.
