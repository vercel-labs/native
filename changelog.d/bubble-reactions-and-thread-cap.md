feature: **Bubble reactions**: `<reactions>` inside a bubble docks the reaction pill — one small muted capsule straddling the bubble's bottom edge with a page-background ring, one text run (`{bindings}` work), dock via `text-alignment` (default `end`) — consumed onto the bubble's chrome-text channels (`text`/`text_alignment` in the Zig builder), engine parity pinned, NSUI riding a fresh registry code (element 65, no schema bump), LSP/docs vocabulary, and damage regions covering the overhang.
- **Bubble thread cap**: a hug-sized bubble now caps at 80% of its container's width (the measured reference treatment) and wraps its message instead of spanning the thread; `ghost` is exempt and an explicit `width` wins. Column-direct bubbles hug their message up to the cap even under stretch alignment, so `cross="end"` right-aligns a sent run without spacer scaffolding.
- **Grouped bubble runs**: documented as spacing, not vocabulary — the reference keeps full capsule corners inside a run, so the recipe is plain columns with 8 gap within a sender's run and 32 between turns (the turn gap doubles as the reaction pill's breathing room).
