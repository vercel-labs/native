improvement: **`list_item` is the flat list-row composite**: it now lays out CHILDREN as a horizontal flow (gap-consuming, cross-aligned like a row) inside its flat wash chrome — no border, no card surface; hover and selection stay full-width edge-to-edge washes — so apps compose indicator/title/meta rows without reaching for bordered panels. The soundboard's track rows moved onto it: the tracks view reads as one flat list (no per-row card chrome, no inter-row gaps) with the now-playing row as a full-width selected wash.
