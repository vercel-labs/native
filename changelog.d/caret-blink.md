improvement: **The text caret blinks**: render animations gained a `loop` flag (ping-pong the from→to sweep forever; a looping animation stays active until removed), and the widget display-list refresh keeps a looping opacity animation on the focused editable's caret command — solid for 500 ms after activity, then fading out and back per sweep, re-armed by every keystroke so the caret holds steady while typing. Blur or focus moving off the field removes the animation so views go idle again.
