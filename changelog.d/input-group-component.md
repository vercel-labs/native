feature: **Input group**: the composer-grade grouped input — `<input-group>` wraps one textarea plus an optional `<input-group-actions>` accessory row inside ONE bordered field; the group wears the focus ring for its focused descendant (focus-within, invalidation included) and the textarea's own chrome dissolves automatically, with `Ui.inputGroup`/`Ui.inputGroupActions` as the Zig-view equivalents, engine parity pinned, NSUI riding fresh registry codes (no schema bump), LSP/docs vocabulary, and a component catalog page with hero tile.
