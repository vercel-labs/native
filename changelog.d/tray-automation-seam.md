feature: **The tray is automation-visible and drivable**: `snapshot.txt` now carries the live status item — a `tray title="ZN 3" items=N` line plus per-row `tray-item #id label="..." command="..." enabled=...` entries (separators included), published from the same runtime state every create/update applies — and `native automate tray-action <item-id>` selects a dropdown row through the exact platform event a real NSStatusItem menu click emits, so command resolution, the `.tray` source, and UiApp's window fallback are all the real path. Unknown or malformed ids degrade loudly into the dispatch-error ring as `automation.tray_action`, like widget-click on a stale target. The menu bar sits outside every window capture, so this closes the last unverifiable surface of the model-driven tray: ovation's open-count badge and latest-issues rows can now be asserted and driven live.
