improvement: **Linux GPU present takes pixels by descriptor pointer**: `native_sdk_gtk_present_gpu_surface_pixels` is reached through one `native_sdk_gtk_gpu_present_desc_t` pointer instead of its wide positional signature, so `rgba8_len` no longer lands a stack slot off under the self-hosted x86_64 backend (which silently rejected the present as a blank canvas). Completes the create/update-view seam fix for the last mixed C-ABI call the platform layer crosses.
