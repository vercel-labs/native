improvement: **The text estimator is now the bundled face's real advance table** (the estimator-vs-real-glyph divergence): printable ASCII advances are read from the embedded Geist `cmap`/`hmtx` at comptime (a bundle swap that drops ASCII coverage is a compile error), every other covered codepoint resolves against the face at runtime, and the flat 0.65em multibyte guess is gone — uncovered East Asian wide/fullwidth/emoji codepoints charge a full em (the CJK fallback-font advance), everything else charges the face's own `.notdef` advance (0.6em). Layout now measures exactly what the reference renderer inks; a macOS test pins estimator-vs-CoreText agreement on the bundled bytes (per-codepoint within 0.001em, kerned UI strings within 2.5%). The dev-2 README pixel golden regenerated (3.78% of pixels, all sub-pixel text shifts and healed em-dash/middot spacing).
- **Estimator hardening**: truncated or malformed UTF-8 clusters take the `.notdef` fallback instead of tripping `std.unicode.utf8Decode`'s assert.
