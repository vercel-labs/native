fix: **Transport totals agree with the track lists**: the soundboard's now-playing bar and the deck's timecode used to adopt the platform player's duration report as their total while the track lists rendered the manifest value, so the same track could show two lengths at once — minutes apart mid-stream (the player extrapolates a progressive stream's length from the bitrate seen so far), and seconds off even from local files, because the prepared catalog ships without a seek header and every platform readout of it is an estimate (the manifest duration is measured from the intact source and matches a full decode exactly). Both examples now keep the manifest total for every display surface, the scrubber fraction, and the seek scale — a seek lands where the user pointed on the displayed timeline — while the platform's report is mirrored into the model (`platform_duration_ms`) and adopted only for a track the manifest carries no duration for. The audio effect channel's docs now state honestly that `loaded` carries the player's estimate, not a measured truth.
