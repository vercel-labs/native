feature: **`pad(x, width)` joins the expression library**: zero-pads the integer value of x to `width` digits (`pad(7, 2)` renders `07`, the sign rides outside the width) — the mm:ss counter formatter. Seventeen functions now; the registry, both engines' shared evaluator, the teaching errors, and the parity/hostile test suites all grew together, and the addition is schema-additive (no NSUI schema bump).
