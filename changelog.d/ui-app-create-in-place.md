fix: **`UiApp.create`/`destroy`: the Model never rides the stack** (the third sighting of the multi-MB-struct-by-value stack-overflow trap): `init`'s by-value Model materializes runtime-built models as caller-stack temporaries — at ovation's 5.9 MB Model, one test's 2 MB scratch beside it overflowed the 8 MiB test stack and segfaulted inside init. `create(backing, options)` heap-allocates the app and constructs every field, the Model included, in place (the Model starts as its default value; boot state is assigned through the returned pointer), `initInPlace` is the seam for models built elsewhere, and the mobile embed host plus the `native init` scaffold and the native-ui skill now model the pattern. A 12 MiB-Model regression test crashes outright if any path puts the Model or the app struct back on the stack — the by-value shape it replaces was verified to segfault on exactly that fixture.
