feature: **iOS audio playback and streaming**: the toolkit-owned UIKit host gains the full audio effect family through a new embed audio service seam (`native_sdk_app_set_audio_service` + `native_sdk_app_audio_event`), porting the macOS AVFoundation shapes — AVAudioPlayer for local files and verified cache entries, AVPlayer for progressive URL streams with honest `buffering` reports, ~500ms position ticks only while playing, one completion at natural end, and the parallel cache-filling download (part file, size-verified against the manifest, atomic rename) landing in the app container's `Library/Caches/audio/` (`app_dirs` now resolves iOS directories inside the sandbox container). The host owns an honest playback audio session: category playback, activated on first play, and a system interruption pauses the transport and reports the paused state through the ordinary position event stream (no auto-resume, no background-audio entitlements). Embed hosts without a registered service now decline `audio_playback`/`audio_streaming` honestly and `fx.playAudio` degrades to one explicit failed Msg instead of a silent fake.
