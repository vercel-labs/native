fix: **Avatar initials center on the circle**: the avatar emitter passed a pre-centered origin AND a `.center` text layout, so the alignment offset applied twice and parked the initials right of the circle center in every renderer (static docs previews, wasm previews, and the live GPU path alike). The origin is now the frame start like every other center-aligned text draw, layout alignment is the one centering pass, and the static avatar previews are regenerated.
