improvement: **Per-component Eject sections in the docs**: the stepper and timeline pages gain an Eject section right after their markup/builder examples — what ejecting hands you, the exact `native eject component <name>` command, and a link to the ownership model in Building components — rendered by one shared `<EjectSection>` so the wording lives in one place. The section resolves names, forms, and destination paths from a new `ejectable` table in the generated vocab JSON, written straight from the CLI's eject registry, so the docs fail the build on any name `native eject component` doesn't accept.
