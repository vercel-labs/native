improvement: **Docs overhauled to native-first**: the docs body now teaches the canvas path the toolkit actually leads with — Quick Start scaffolds `native init`'s native template (markup + Model/Msg/update, hot reload, markup check, automation assert), App Model teaches the update loop plainly instead of the WebView `App` struct, and two new pages land: State & Data Flow (derive-don't-store, binding resolution, the text-field mirror, the echo pattern, the clock seam) and App & Runtime (the relocated lower-level `App`/`Runtime` reference).
- **Native-first information architecture**: the sidebar reorganizes into Get Started → Core Concepts → Components → Native Platform → Automation & Testing → Packaging & Distribution → Mobile & Embedding → Web Content → Reference; the WebView/bridge/frontend pages move under Web Content with honest framing (embedding web content in a native app), and every page that presented the WebView as the default app model is reframed.
- Stale commands and API shapes fixed along the way: `zig build run-webview` → `zig build run`, `automate screenshot` documents its `<view-label> [scale]` signature and PNG artifact (the 2x2 PPM placeholder note is gone), `markup check`/`markup lsp` join the CLI reference, the `TestHarness` sample uses the real heap-allocating `create`/`destroy` shape, and the app.zon reference leads with the manifest `native init` actually generates.
