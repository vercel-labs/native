improvement: **Teaching-error and docs-truth batch from cold-start authoring runs**: binding a `TextBuffer` model field directly now teaches the fn-over-buffer pattern in both engines and the checker; quoted template arg defaults get a teaching error (bare `name=` is the documented empty default); the a11y check reports every finding per file instead of the first; `native check` hints when a failing `src/*.native` file is embedded by no Zig source; the model-contract staleness note says `native test` (the command that exists in zero-config apps); the dead-state lint names Zig-built views as legitimate `view_unbound` reasons.
- **Docs match the engine on form controls**: radio/checkbox labels ride `text=` (text content is rejected), slider `on-change` is payload-less with the `Options.sync` mirror documented, `msgForValue` is builder-only (`msgFor(id, .change)` is the markup-slider path), and the habits example now carries the radio-group pattern in real markup.
- **Hybrid composition documented**: a Zig builder root composing `CompiledMarkupView` fragments (charts-in-markup-apps) has its own section, including the Debug hot-reload wiring for embedded fragments.
