feature: **Icon registry expansion (41 → 45)**: the remaining demanded GitHub vocabulary lands — `git-merge`, `git-branch`, `check-circle`, `x-circle`, the PR/issue state names GitHub-shaped apps kept demanding — available in markup (`<icon name>`, `<button icon>`) and Zig views (`Ui.icon`, `ElementOptions.icon`), with the validator mirror, LSP, docs, and skill lists updated in lockstep.
