fix: **`native automate` payloads land on stdout**: snapshot, list, wait, bridge, and screenshot output — everything a script exists to parse — now goes to stdout, so `native automate snapshot > file` and `| grep` finally work; diagnostics and progress (queued confirmations, assert reporting, errors, usage) stay on stderr, per the Unix contract. `native version` moved to stdout for the same reason. Scripts that captured `2>&1` keep working unchanged.
