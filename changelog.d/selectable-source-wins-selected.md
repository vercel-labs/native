fix: **A model-side selection move no longer leaves two washed rows**: exclusive selectables (`list_item`, `menu_item`, `data_cell`, `segmented_control`, `radio` — tree rows included) retained their runtime `selected` unconditionally, so when the model moved its selection (clear row A, select row B in one update) the old row kept its wash and `state=[selected]` beside the new one until the next pointer interaction. The selected bool now follows the slider/scroll reconcile rule: a source-side FLIP wins — an explicit `selected = false` clears the retained echo, the new row lights from the source — while a source that replays the same value keeps the retained (pointer-driven) selection, so uncontrolled lists and trees keep working with zero app wiring. Regression-tested through a rebuild-driven selection move between two treeitems (exactly one wash, semantics agree) plus the uncontrolled retained-wins contract.
