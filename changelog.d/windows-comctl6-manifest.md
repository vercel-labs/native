fix: **Windows apps launch on real Windows**: every app executable now embeds a common-controls v6 manifest, and the message dialog resolves TaskDialogIndirect dynamically (falling back to MessageBoxW on v5). The v6-only static import previously killed every app at load time with STATUS_ENTRYPOINT_NOT_FOUND on stock Windows 11 — the CI emulation layer exports the symbol unconditionally, so only a real machine caught it.
