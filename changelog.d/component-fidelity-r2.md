improvement: **Accordion, tabs, alert, and card land the house look by default**: accordion items are borderless rows divided by hairline separators, with the registry `chevron-down` icon on the trailing edge rotating 180° when expanded, and they finally SIZE THEMSELVES (header band collapsed, header + content expanded — no more hand-managed per-state heights); the `tabs` container renders the house tab-strip treatment (ONE muted rounded box with 3px padding hugging its triggers) while unselected `segmented_control` triggers go transparent so the container provides the wash; the alert's mark is the registry `info` icon (the warning triangle on `destructive`, which now also reads in the destructive hue) at a fixed 16px, centered on the first text line, and alert children render as the DESCRIPTION — starting under the title and hanging past the icon column, the standard callout grid; cards carry 24px of content padding out of the box (16 at `size="sm"`) and grow around their children instead of clipping them at the 120pt floor.
- **Per-kind ergonomic layout defaults are one shared table** (`canvas.widgetKindDefaultLayout`), consulted by `builtinComponentWidget` AND the ui builder (both markup engines build through it), applied only when the author left padding/gap untouched — explicit spacing always wins.
