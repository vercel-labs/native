perf: **One display-list emission per input cycle**: the gpu-surface input path's refresh batch now spans the app dispatch too, so a pointer-up's widget-state refresh and the Msg-driven rebuild's refresh (whose `setCanvasWidgetLayout` path is batch-aware) coalesce into a single emission with the same final display list — a click used to build the same list up to three times (pointer-down refresh, pointer-up refresh, rebuild refresh). The automation `widget-click` verb additionally wraps its synthetic down+up pair in one outer batch (the per-input batches nest), since no frame can present between the two synchronous events; live hosts still paint press feedback because a real down and up arrive as separate events.
