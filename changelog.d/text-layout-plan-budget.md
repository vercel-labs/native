improvement: **Text-layout plan limits raised and made loud**: the per-frame plan budget is now derived from the command budget (512 -> 2048, `max_canvas_text_layouts_per_view = max_canvas_commands_per_view`), so `TextLayoutPlanListFull` is structurally unreachable through a real display list — a long agent transcript can no longer kill renders at the plan list. The shared wrapped-line pool gets its own budget (`max_canvas_text_layout_lines_per_view` = 8192, ~10x the worst measured real view); overflow there stays a loud error with a teaching diagnostic naming the budget and the telemetry field.
- **Text-layout headroom telemetry**: every gpu_surface snapshot view line reports `text_layout_plans=N/2048 text_layout_lines=N/8192` beside the widget-budget fields, so the cliff is visible while authoring instead of at a dead frame.
