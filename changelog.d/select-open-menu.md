fix: **The open select menu reads like a menu**: option rows never draw a focus outline — while a menu is open, Up/Down (and hover) move a full-row highlight, and the committed option wears a trailing checkmark at the right edge of its row instead of a selected wash, so "where the keyboard is" and "what is committed" stay visibly independent. Arrowing is provisional: it never dispatches the row's press. Enter or a click commits (exactly one `on-press`, the model closes); Escape, a click outside, and now Tab (focus departure — scoped to menu surfaces, never popovers) dismiss through `on-dismiss` with the committed value untouched and focus handed back to the trigger.
- Menu rows sit on a comfortable 32px band and reserve the trailing checkmark slot on every row, so labels measure and elide identically whether or not they are committed.
- The select trigger's open-below affordance is the registry `chevron-down` icon at the shared row-icon extent, tinted muted like the placeholder register.
