fix: **Model-driven slider values render again**: sliders retained their runtime value unconditionally across rebuilds, so a source-driven `value=` binding (playback progress on a seek bar) froze at whatever the runtime last held. Sliders now follow the scroll reconcile rule — a source-side MOVE wins, a source replaying the same value keeps the retained user drag — and a live drag is never yanked: while the thumb is pressed, a mid-gesture source tick keeps the dragged value (the view's pointer press is stamped into the reconcile entries), with the source winning again after release.
